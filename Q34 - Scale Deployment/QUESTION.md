# Question 34 – Scale Deployment

**Context:** A Deployment named `scale-app` with 2 replicas is already running in namespace `q34`. You need to scale it up to handle increased traffic.

## Your Task

1. Scale the Deployment `scale-app` in namespace `q34` to `6` replicas.

## Docs

- [Scaling a Deployment](https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#scaling-a-deployment)
